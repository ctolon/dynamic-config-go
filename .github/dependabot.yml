version: 2

updates:
  # Two direct dependencies, kept current on purpose: a small graph is only
  # an advantage if somebody is watching it.
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
